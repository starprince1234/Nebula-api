import { mount, flushPromises } from '@vue/test-utils'
import { describe, expect, it, vi } from 'vitest'
import TeacherModels from './TeacherModels.vue'

vi.mock('vue-router',()=>({useRoute:()=>({query:{}}),useRouter:()=>({replace:vi.fn()})}))
vi.mock('../../api/teacher',()=>({teacherAPI:{models:vi.fn().mockResolvedValue([{model_id:'m1',display_name:'M1',description:null,category:'text',capabilities:[],input_modalities:[],output_modalities:[],context_window:null,max_input_tokens:null,max_output_tokens:null,is_common:true,status:'active',route_ready:true,credit_multiplier:'1.000'}]),providers:vi.fn().mockResolvedValue([]),model:vi.fn(),catalogCandidates:vi.fn().mockResolvedValue({items:[],page:1,page_size:20,total:0}),createModel:vi.fn(),updateModel:vi.fn(),createBinding:vi.fn(),updateBinding:vi.fn(),probeModel:vi.fn()}}))

describe('TeacherModels',()=>{it('shows total model count, four steps and an operable probe panel',async()=>{const wrapper=mount(TeacherModels,{global:{stubs:{LoadingRegion:{template:'<div><slot/></div>'},AppDialog:{props:['open','title'],template:'<div v-if="open"><slot/></div>'},StatusBadge:true}}});await flushPromises();expect(wrapper.text()).toContain('全部模型 1');await wrapper.get('button.button.primary').trigger('click');for(const label of ['模型标识','基础配置','Binding','启用确认'])expect(wrapper.text()).toContain(label);const inputs=wrapper.findAll('input');await inputs[1].setValue('new-model');await inputs[2].setValue('New Model');await wrapper.findAll('button').find(button=>button.text()==='下一步')!.trigger('click');await wrapper.findAll('button').find(button=>button.text()==='一键配置')!.trigger('click');expect(wrapper.text()).toContain('注册供应商')})})
